# What I need to set up?
Requires node, npm, git, bower, gulp.

### Bower
```sh
npm install -g bower
```

### Gulp
```sh
npm install -g gulp
```

# How do I get set up?

```sh
npm install
bower install
gulp dev
```